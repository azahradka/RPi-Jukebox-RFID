# https://docs.pytest.org/en/6.2.x/reference.html#confval-testpaths

[pytest]
addopts = --maxfail=2 -rf --ff -v --import-mode=importlib -m "not live_api"
testpaths = test
markers =
    live_api: tests that hit the real Spotify API (run with: pytest -m live_api)